export default function Divider() {
  const styles = {
    divider: {
      minWidth: '0.25vw',
      height: '100%',
      backgroundColor: 'var(--gr)'
    }
  }

  return (
    <div style={styles.divider} />
  )
}